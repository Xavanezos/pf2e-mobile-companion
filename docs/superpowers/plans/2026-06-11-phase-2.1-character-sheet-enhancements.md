# Phase 2.1 — Character Sheet Enhancements — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-06-11-phase-2.1-character-sheet-enhancements-design.md`

Conventions match Phase 2: relative imports; pure logic is TDD; components verified by `npm run typecheck` + `npm run build`; one commit per task to `main`, messages `Phase 2.1: …` with the `Co-Authored-By: Claude Opus 4.8 (1M context)` trailer.

**Shipped before this plan:** hero-point pips fix (`8a71e9c`), shield-HP fix (`b6d1581`).

---

## Task A — Reorg: "Profs" sub-tab (store + bar)
- Modify `src/app/store.ts`: add `"profs"` to `SheetSubTab`.
- Modify `src/app/sheet/SubTabBar.tsx`: insert `{ id: "profs", label: "Profs" }` before `bio`.
- Gate: typecheck + build. Commit.

## Task B — Reorg: Vitals gains Character + Languages
- Modify `src/app/sheet/VitalsPanel.tsx`: add a top **Character** section (ancestry/heritage/background/class/deity/size from `view.bio`) and a **Languages** section (`view.bio.languages`).
- Gate: typecheck + build. Commit.

## Task C — Reorg: ProfsPanel + Bio prose-only
- Create `src/app/sheet/ProfsPanel.tsx`: weapon (`view.bio.attacks`) + armor (`view.bio.defenses`) proficiencies with `RankPip`; empty-state if none.
- Modify `src/app/sheet/CharacterSheet.tsx`: import + render `{subTab === "profs" && <ProfsPanel view={view} />}`.
- Modify `src/app/sheet/BioPanel.tsx`: strip to Appearance + Backstory only (remove Character/Languages/Proficiencies sections).
- Gate: typecheck + build. Commit.

## Task D — #5 data: modifier breakdowns (TDD)
- Modify `src/foundry/actor/types.ts`: add `ModPartView { label; value }`, `ModifierLike { label; modifier; enabled?; type? }`; add optional `breakdown?` to `SaveView`/`SkillView`/`ClassDcView`/`DefensesView.perception`, `acBreakdown?` to `DefensesView`; add optional `modifiers?` to `CharacterLike` `attributes.ac`/`saves[x]`/`perception`/`classDCs[x]` and `SkillLike` (`modifiers?` + `check?.modifiers?`).
- Modify `src/foundry/actor/view.ts`: add `readBreakdown(stat)` helper (`stat.modifiers ?? stat.check?.modifiers ?? []`, filter `enabled !== false`, map `{label, value: modifier}`); wire into `mapDefenses` (ac/saves/perception/classDCs) and `mapSkills`.
- Tests: extend `tests/fixtures/characterLike.ts` with sample modifiers; new `tests/characterView.breakdown.test.ts` (enabled filter, label/value, missing-array → []).
- Gate: `npx vitest run`, typecheck, build. Commit.

## Task E — #5 UI: BreakdownModal + tappable numbers
- Create `src/app/sheet/BreakdownModal.tsx`: props `{ title; total; parts: ModPartView[]; onClose; onRoll? }`; lists `label … +value` rows + a Total row; renders an optional Roll button only if `onRoll` given (Phase-3 seam).
- Modify `CharacterSheet.tsx`: `breakdown` state `{title,total,parts}|null`; `onShowBreakdown` passed to Vitals + Skills; render `BreakdownModal`.
- Modify `VitalsPanel.tsx`: make AC/saves/perception/classDC `StatRow`s call `onShowBreakdown` (StatRow already supports `onClick`).
- Modify `SkillsPanel.tsx`: make each skill `StatRow` call `onShowBreakdown`.
- Gate: typecheck + build. Commit.

## Task F — #3 data: item/feat detail (TDD)
- Create `src/foundry/enrich.ts`: `enrichHtml(html)` best-effort (`TextEditor.enrichHTML` global or `foundry.applications.ux…`, fallback raw).
- Modify `src/foundry/actor/types.ts`: add `ItemDetailView` and `ItemDetailLike`; add `id` to `EffectView` + `EffectLike`.
- Modify `src/foundry/actor/view.ts`: add `buildItemDetail(it: ItemDetailLike): ItemDetailView` (typeLabel, level, traits, actionGlyph, meta lines for qty/bulk/price/usage, raw descriptionHtml); add `id` in `mapEffects`.
- Tests: new `tests/itemDetail.test.ts` (feat → action glyph + level; item → bulk/price/qty meta; missing fields safe).
- Gate: vitest, typecheck, build. Commit.

## Task G — #3 UI: DetailModal + wire taps
- Create `src/app/sheet/DetailModal.tsx`: props `{ actorId; itemId; onClose }`; reads `game.actors.get(actorId).items.get(itemId)`, `buildItemDetail`, enriches description async (best-effort), renders in `Modal` (header: name/type/level/traits/glyph; meta line; description HTML via `dangerouslySetInnerHTML`).
- Modify `CharacterSheet.tsx`: `detailItemId` state; `onShowDetail(id)` passed to Feats/Items/Vitals(effects); render `DetailModal`.
- Modify `FeatsPanel.tsx`: feat row → `onShowDetail(f.id)`.
- Modify `ItemsPanel.tsx`: item name/img area → `onShowDetail(item.id)` (equip/invest buttons unaffected).
- Modify `VitalsPanel.tsx`: effect chips → `onShowDetail(e.id)`.
- Gate: typecheck + build. Commit.

## Task H — Close-out: roadmap + spec status
- Modify `pf2e-mobile-companion-plan.md`: mark Phase 2 ✅ done (verified) and add a **Phase 2.1** section (2 fixes + 3 features + reorg) referencing this spec/plan.
- Commit.

---

## Manual test checklist (for the user's return)
See the session hand-off / final message. Covers: hero pips visible + adjust; shield HP persists; Profs tab + Vitals Character/Languages + Bio prose-only; tap feat/item/effect → detail; tap AC/save/perception/skill/classDC → breakdown. Plus the live-API paths to confirm (spec "Live-API assumptions").
